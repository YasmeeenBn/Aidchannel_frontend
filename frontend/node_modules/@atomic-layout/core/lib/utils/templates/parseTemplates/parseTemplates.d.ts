import { AreasList } from '../getAreasList';
import { Props } from '../../strings/parsePropName';
declare type ParseTemplates = (props: Props) => AreasList;
/**
 * Parses a given map of props and returns an areas map.
 */
declare const parseTemplates: ParseTemplates;
export default parseTemplates;
