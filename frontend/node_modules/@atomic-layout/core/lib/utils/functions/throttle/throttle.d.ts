/**
 * Throttles a given function.
 * Implements both leading and trailing function calls.
 */
export default function throttle<F extends (...args: any[]) => any>(func: F, interval: number): (...args: Parameters<F>) => void;
