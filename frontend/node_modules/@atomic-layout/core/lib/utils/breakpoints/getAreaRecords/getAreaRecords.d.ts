import { Breakpoint, BreakpointBehavior } from '../../../const/defaultOptions';
import { Template } from '../../templates/getAreasList';
export interface AreaRecord {
    breakpoint: Breakpoint;
    behavior: BreakpointBehavior;
}
declare type AreaRecordsList = Array<AreaRecord | null>;
export default function getAreaRecords(areaName: string, templates: Template[]): AreaRecordsList;
export {};
